package GB_2_lesson1;

public class RoadTrack extends Obstacle {
    private int distance;

    public RoadTrack(int distance) {
        this.distance = distance;
    }

    @Override
    public void doIt(Competitor competitor) {
        competitor.run(distance);
    }
}
